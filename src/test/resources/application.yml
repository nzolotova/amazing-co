spring:
  jpa:
    generate-ddl: true
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

logging:
  level:
    org.hibernate.tool.hbm2ddl: 'TRACE'
    org.hibernate.SQL: 'DEBUG'